# MCOS 组播插件

由 Supervisor 管理的 **multicast** 插件容器，面向 **MCOS**；发布为 **`ghcr.io/muthur-command/{arch}-mcio-multicast`**（多架构清单 **`mcio-multicast`**）。

基于 **`ghcr.io/muthur-command/base`** 构建；OCI 标签使用 **`io.mcio.*`**。

## 许可证

见 **LICENSE**（Apache-2.0；按 fork 要求保留上游版权 / NOTICE）。
